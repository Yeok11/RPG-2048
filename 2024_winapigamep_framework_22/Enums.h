#pragma once
enum class LAYER
{
	DEFAULT = 0,
	BACKGROUND,
	EMPTY_TILE,
	OBJECT_TILE,
	ENEMY,
	UI,
	END = 30
};

enum class OBJ_TYPE
{
	NONE = 0,
	MAIN,
	EMPTY,
	NORMAL,
	STONE,
	//WALL
};

typedef enum class CALCULATE
{
	PLUS = 0,
	MINUS,
	MULTIPLY,
	DIVIDE
} CALC;



enum class PEN_TYPE
{
	HOLLOW, RED, GREEN,
	BLUE, YELLOW, END
};

enum class BRUSH_TYPE
{
	HOLLOW, RED, GREEN,
	BLUE, YELLOW, END
};


enum class EVENT_TYPE
{
	CREATE_OBJECT,
	DELETE_OBJECT,
	SCENE_CHANGE,
	END,
};

enum class EASE {
	LINER, 
	QUAD_IN, QUAD_OUT, QUAD_INOUT,
	CUBIC_IN, CUBIC_OUT, CUBIC_INOUT,
	SINE_IN, SINE_OUT, SINE_INOUT,
	BOUNCE_OUT, ELASTIC_OUT
};
