#pragma once
enum class LAYER
{
	DEFAULT = 0,
	BACKGROUND,
	NONE_TILE,
	OBJECT_TILE,
	ENEMY,
	UI,
	END = 30
};

enum class OBJ_TYPE
{
	NONE = 0,
	MAIN,
	EMPTY,
	NORMAL,
	STONE,
	//WALL
};

typedef enum class CALCULATE
{
	PLUS = 0,
	MINUS,
	MULTIPLY,
	DIVIDE
} CALC;



enum class PEN_TYPE
{
	HOLLOW, RED, GREEN,
	BLUE, YELLOW, END
};

enum class BRUSH_TYPE
{
	HOLLOW, RED, GREEN,
	BLUE, YELLOW, END
};


enum class EVENT_TYPE
{
	CREATE_OBJECT,
	DELETE_OBJECT,
	SCENE_CHANGE,
	END,
};
