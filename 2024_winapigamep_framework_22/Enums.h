#pragma once
enum class LAYER
{
	DEFAULT,
	BACKGROUND,
	PLAYER,
	PROJECTILE,
	ENEMY,
	// UI
	END = 30
};

enum class OBJ_TYPE
{
	NONE = 0,
	MAIN,
	NORMAL,
	STONE,
	WALL
};

typedef enum class CALCULATE
{
	PLUS = 0,
	MINUS,
	MULTIPLY,
	DIVIDE
} CALC;



enum class PEN_TYPE
{
	HOLLOW, RED, GREEN,
	BLUE, YELLOW, END
};

enum class BRUSH_TYPE
{
	HOLLOW, RED, GREEN,
	BLUE, YELLOW, END
};


enum class EVENT_TYPE
{
	CREATE_OBJECT,
	DELETE_OBJECT,
	SCENE_CHANGE,
	END,
};
